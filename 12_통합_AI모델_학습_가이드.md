# STEP 10 (전면 개편판) — YOLOv8 기반 4개 모델 통합 학습 가이드

> 이전 `10_AI모델_학습_가이드.md`는 이 문서로 대체됩니다. EfficientNet 분류 방식 대신
> **YOLOv8 객체탐지**로 통일합니다. 이유: 3개 데이터셋이 모두 바운딩 박스(불량 위치) 라벨을
> 포함하고 있어서, 분류만 하는 것보다 위치까지 실제로 찾아내는 게 더 쉽고 강력합니다.
> (불량이 하나도 안 잡히면 자동으로 "정상" 처리 — 별도의 정상 데이터가 없어도 됩니다.)

## 만들 모델 4개

| 모델 | 역할 | 데이터셋 |
|------|------|---------|
| ① 카테고리 분류기 | 사진을 보고 용접/표면/조립 중 무엇인지 판별 | 3개 데이터셋의 이미지를 합쳐서 사용 |
| ② 용접 결함 탐지 | 용접 불량 위치 탐지 | Kaggle welding-defect-object-detection (이미 다운로드함) |
| ③ 표면 결함 탐지 | 표면 불량(녹/스크래치 등) 위치 탐지 | Roboflow NEU surface defect |
| ④ 조립 결함 탐지 | 볼트·너트 누락 위치 탐지 | Roboflow missing-bolt-detection |

---

## 0단계 — 새 Colab 노트북 + GPU 설정

1. [colab.research.google.com](https://colab.research.google.com) → 새 노트북
2. 수정 → 노트북 설정 → 하드웨어 가속기 **T4 GPU** → 저장

**셀 1** (필요한 패키지 설치):
```python
!pip install -q ultralytics onnx
import ultralytics
ultralytics.checks()
```

---

## 1단계 — 데이터셋 3개 준비

### 1-1. 용접 데이터셋 (이미 다운로드한 zip)
**셀 2**:
```python
from google.colab import files
print("용접 데이터셋 zip을 선택하세요")
uploaded = files.upload()
import zipfile
zip_name = list(uploaded.keys())[0]
with zipfile.ZipFile(zip_name, 'r') as z:
    z.extractall('welding_raw')
```

### 1-2. 표면 데이터셋 (Roboflow에서 새로 다운로드 필요)

1. 브라우저에서 [universe.roboflow.com/data-set-fptet/neu-surface-defect](https://universe.roboflow.com/data-set-fptet/neu-surface-defect) 접속
2. 우측 **Download Dataset** 클릭 (Roboflow 무료 계정 로그인 필요 — 없으면 Google 계정으로 간단 가입)
3. Format: **YOLOv8** 선택 → **Download zip to computer** 선택 → 다운로드

**셀 3** (다운로드한 표면 데이터셋 zip 업로드):
```python
print("표면 데이터셋 zip을 선택하세요")
uploaded2 = files.upload()
zip_name2 = list(uploaded2.keys())[0]
with zipfile.ZipFile(zip_name2, 'r') as z:
    z.extractall('surface_raw')
```

### 1-3. 조립 데이터셋 (Roboflow에서 새로 다운로드 필요)

1. [universe.roboflow.com/kavyasruthi-loakx/missing-bolt-detection](https://universe.roboflow.com/kavyasruthi-loakx/missing-bolt-detection) 접속
2. **Download Dataset** → Format **YOLOv8** → 다운로드

**셀 4**:
```python
print("조립 데이터셋 zip을 선택하세요")
uploaded3 = files.upload()
zip_name3 = list(uploaded3.keys())[0]
with zipfile.ZipFile(zip_name3, 'r') as z:
    z.extractall('assembly_raw')
```

> 압축을 풀면 보통 `train/images`, `train/labels`, `valid/images`, `valid/labels`, `data.yaml`이 들어있습니다. 폴더 구조가 다르면 알려주세요 — 맞춰드리겠습니다.

---

## 2단계 — YOLOv8 학습 (3개 모델 공통 코드)

각 데이터셋 폴더 안의 `data.yaml` 경로를 그대로 사용합니다.

**셀 5** (data.yaml 위치 확인):
```python
import glob
for name in ['welding_raw', 'surface_raw', 'assembly_raw']:
    yamls = glob.glob(f'{name}/**/data.yaml', recursive=True)
    print(name, '→', yamls)
```

출력된 경로를 아래 코드의 `WELDING_YAML`, `SURFACE_YAML`, `ASSEMBLY_YAML`에 정확히 넣으세요.

**셀 6** (용접 모델 학습 — 약 10~15분):
```python
from ultralytics import YOLO

WELDING_YAML = "welding_raw/data.yaml"  # 셀5 출력값으로 수정

model_welding = YOLO("yolov8n.pt")  # 가벼운 nano 모델
model_welding.train(
    data=WELDING_YAML,
    epochs=40,
    imgsz=640,
    batch=16,
    name="welding_model",
    patience=10,
)
```

**셀 7** (표면 모델 학습):
```python
SURFACE_YAML = "surface_raw/data.yaml"  # 셀5 출력값으로 수정

model_surface = YOLO("yolov8n.pt")
model_surface.train(
    data=SURFACE_YAML,
    epochs=40,
    imgsz=640,
    batch=16,
    name="surface_model",
    patience=10,
)
```

**셀 8** (조립 모델 학습):
```python
ASSEMBLY_YAML = "assembly_raw/data.yaml"  # 셀5 출력값으로 수정

model_assembly = YOLO("yolov8n.pt")
model_assembly.train(
    data=ASSEMBLY_YAML,
    epochs=40,
    imgsz=640,
    batch=16,
    name="assembly_model",
    patience=10,
)
```

각 학습이 끝나면 `mAP50` 점수가 출력됩니다. 0.5 이상이면 합격선입니다 (데이터가 적으면 더 낮아도 괜찮습니다 — 포트폴리오 단계에서는 "작동한다"가 중요).

---

## 3단계 — 카테고리 분류기 학습 (용접/표면/조립 판별)

3개 데이터셋의 이미지를 모아서 "이 사진이 어느 도메인인지"만 분류하는 가벼운 모델을 만듭니다.

**셀 9** (카테고리별 이미지 모으기):
```python
import os, glob, shutil, random
from pathlib import Path

cat_dir = Path('category_data')
for cat in ['welding', 'surface', 'assembly']:
    (cat_dir / cat).mkdir(parents=True, exist_ok=True)

sources = {
    'welding':  glob.glob('welding_raw/**/images/*.*', recursive=True),
    'surface':  glob.glob('surface_raw/**/images/*.*', recursive=True),
    'assembly': glob.glob('assembly_raw/**/images/*.*', recursive=True),
}

for cat, files_list in sources.items():
    sample = random.sample(files_list, min(300, len(files_list)))  # 도메인당 최대 300장
    for f in sample:
        shutil.copy(f, cat_dir / cat / Path(f).name)
    print(cat, len(sample), "장 복사 완료")
```

**셀 10** (EfficientNet-B0로 카테고리 분류기 학습):
```python
import torch, torch.nn as nn
from torch.utils.data import Dataset, DataLoader, random_split
from torchvision import transforms, models
from PIL import Image

device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')

class CategoryDataset(Dataset):
    def __init__(self, root, transform=None):
        self.samples = []
        self.classes = ['welding', 'surface', 'assembly']
        for idx, cls in enumerate(self.classes):
            for f in glob.glob(f'{root}/{cls}/*'):
                self.samples.append((f, idx))
        self.transform = transform
    def __len__(self): return len(self.samples)
    def __getitem__(self, idx):
        path, label = self.samples[idx]
        img = Image.open(path).convert('RGB')
        if self.transform: img = self.transform(img)
        return img, label

transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),
])

full_ds = CategoryDataset('category_data', transform=transform)
n_val = int(len(full_ds) * 0.2)
train_ds, val_ds = random_split(full_ds, [len(full_ds) - n_val, n_val])
train_loader = DataLoader(train_ds, batch_size=32, shuffle=True)
val_loader   = DataLoader(val_ds, batch_size=32)

model_cat = models.efficientnet_b0(weights=models.EfficientNet_B0_Weights.DEFAULT)
model_cat.classifier[1] = nn.Linear(model_cat.classifier[1].in_features, 3)
model_cat = model_cat.to(device)

criterion = nn.CrossEntropyLoss()
optimizer = torch.optim.Adam(model_cat.parameters(), lr=1e-4)

for epoch in range(8):
    model_cat.train()
    correct, total = 0, 0
    for imgs, labels in train_loader:
        imgs, labels = imgs.to(device), labels.to(device)
        optimizer.zero_grad()
        out = model_cat(imgs)
        loss = criterion(out, labels)
        loss.backward(); optimizer.step()
        correct += (out.argmax(1) == labels).sum().item()
        total += labels.size(0)

    model_cat.eval()
    vcorrect, vtotal = 0, 0
    with torch.no_grad():
        for imgs, labels in val_loader:
            imgs, labels = imgs.to(device), labels.to(device)
            out = model_cat(imgs)
            vcorrect += (out.argmax(1) == labels).sum().item()
            vtotal += labels.size(0)
    print(f"Epoch {epoch+1}/8 | 학습정확도 {correct/total:.2%} | 검증정확도 {vcorrect/vtotal:.2%}")
```

---

## 4단계 — 4개 모델 모두 ONNX로 변환 + 다운로드

**셀 11**:
```python
import os

os.makedirs('exported_models', exist_ok=True)

# YOLOv8 모델 3개 → ONNX
model_welding.export(format='onnx', imgsz=640)
model_surface.export(format='onnx', imgsz=640)
model_assembly.export(format='onnx', imgsz=640)

# 변환된 onnx 파일 찾아서 exported_models로 이동
import glob, shutil
for name, label in [('welding_model', 'welding'), ('surface_model', 'surface'), ('assembly_model', 'assembly')]:
    found = glob.glob(f'runs/detect/{name}*/weights/best.onnx')
    if found:
        shutil.copy(found[0], f'exported_models/{label}.onnx')
        print(label, '→ exported_models/' + label + '.onnx')

# 카테고리 분류기 → ONNX
model_cat.eval()
dummy = torch.randn(1, 3, 224, 224).to(device)
torch.onnx.export(
    model_cat, dummy, 'exported_models/category.onnx',
    input_names=['input'], output_names=['output'],
    dynamic_axes={'input': {0: 'batch_size'}, 'output': {0: 'batch_size'}},
    opset_version=12,
)
print('category → exported_models/category.onnx')
```

**셀 12** (4개 파일을 zip으로 묶어서 한 번에 다운로드):
```python
shutil.make_archive('ai_models', 'zip', 'exported_models')
files.download('ai_models.zip')
```

다운로드된 `ai_models.zip` 안에 `welding.onnx`, `surface.onnx`, `assembly.onnx`, `category.onnx` 4개 파일이 들어있어야 합니다.

---

## 5단계 — 완료 후

`ai_models.zip` 다운로드가 끝나면 알려주세요. 그 다음 작업:

1. 4개 onnx 파일을 `backend/models/weights/` 폴더에 저장
2. FastAPI에 `real_model.py` 작성 — onnxruntime으로 4개 모델 순서대로 추론
   (① category.onnx로 도메인 판별 → ② 판별된 도메인의 YOLO 모델로 불량 탐지)
3. `requirements.txt`에 `onnxruntime`, `numpy` 추가
4. Render는 무료 플랜이라 모델 파일이 크면 git에 올리기 부담될 수 있음 — 이 부분은 모델 받고 나서 크기 보고 같이 결정

---

## 막히면

- **Roboflow 다운로드가 막힘/유료라고 나옴**: 무료 계정도 YOLOv8 포맷 다운로드 가능합니다. 로그인 후 다시 시도.
- **학습 중 `CUDA out of memory`**: `batch=16`을 `batch=8`로 낮추세요.
- **mAP50이 너무 낮음(0.1 이하)**: epochs를 80으로 늘리거나, 데이터셋의 `data.yaml` 안 클래스 수가 맞는지 확인.
- **각 zip 구조가 가이드와 다름**: 압축 풀고 나온 폴더 구조 스크린샷 보여주시면 코드 맞춰드립니다.
